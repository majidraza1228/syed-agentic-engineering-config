---
description: Show token usage for the current window and recent session history
argument-hint: "[reset]"
allowed-tools: Bash
---

# /tokens — Token usage

!`$HOME/src/agentic-config/token-report.sh $ARGUMENTS`

Print the output above exactly as-is. No additional commentary.
